{
  "name": "whatsapp-shopify-webhook-vercel",
  "version": "1.0.0",
  "description": "WhatsApp Business API integration with Shopify webhooks on Vercel",
  "main": "api/webhook.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "start": "echo 'Use vercel dev for local development'"
  },
  "dependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}